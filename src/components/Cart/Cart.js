import styles from "./Cart.module.css";

const Cart = () => {
  return (
    <div className={styles.cart}>
      <div className={styles["cart-content"]}></div>
    </div>
  );
};

export default Cart;
